{% extends 'admin.twig' %}


{% block body %}
	{% embed 'admin/layouts/header.twig' %}
		{% block headerh %}
			{% if type_link is defined %}
				Edit {{h1}}
			{% else %}
				Add {{h1}}
			{% endif %}
		{% endblock %}

        {% block header_button %}
			<a href="javascript:void(0);" data-target="#modal-config-tabs" data-toggle="modal" class="btn"><i class="fa fa-gears"></i></a>
            <a href="{{ path_for(all_e_link) }}" class="btn btn-default">Back to {{h1}}</a>
        {% endblock %}
    {% endembed %}

    {% include 'admin/layouts/adddata.twig' %}

	{% include 'admin/layouts/footer.twig' %}
{% endblock %}

{% block footerAdditionalScripts %}
<script src="/js/tinymce/tinymce.min.js"></script>
<script>$(document).ready(function(){tinymce.init({ selector:'textarea.tinymce' });});</script>
{% endblock %}
